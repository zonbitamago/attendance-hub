# Implementation Plan: イベント画面 個人別出欠状況表示機能

**Branch**: `007-event-member-attendance` | **Date**: 2025-11-10 | **Spec**: [spec.md](./spec.md)
**Input**: Feature specification from `/specs/007-event-member-attendance/spec.md`

**Note**: This template is filled in by the `/speckit.plan` command. See `.specify/templates/commands/plan.md` for the execution workflow.

## Summary

イベント詳細画面に、グループメンバーの個人別出欠状況を表示する機能を追加する。現在はグループごとの人数集計のみだが、これをアコーディオンUI化し、展開時に個人名と出欠ステータス（◯/△/✗/-）を表示する。さらに、フィルタリング（ステータス別）、ソート（名前順/ステータス順）、検索（メンバー名）機能を実装する。

**技術アプローチ**:
- 既存のイベント詳細ページ（`app/[org]/events/[id]/page.tsx`）を拡張
- グループ別集計セクションをアコーディオンコンポーネントに置き換え
- `lib/attendance-service.ts`に新規関数`getGroupMemberAttendances()`を追加
- 再利用可能なコンポーネントを`components/event-detail/`に配置
- React状態管理（useState）でアコーディオン、フィルタ、ソート、検索の状態を管理

## Technical Context

**Language/Version**: TypeScript 5.9 (strict mode必須)
**Primary Dependencies**: Next.js 16.0.1, React 19.2.0, Tailwind CSS 3.4
**Storage**: localStorage（プロトタイプ用、将来的にSupabase PostgreSQLへ移行予定）
**Testing**: Jest 29以上, React Testing Library 14以上
**Target Platform**: Webブラウザ（モバイル・タブレット・デスクトップ対応）
**Project Type**: web（Next.js App Router）
**Performance Goals**: 100人以上のメンバーで3秒以内のレスポンス、アコーディオン操作1秒以内
**Constraints**: クライアントサイドレンダリング、localStorage使用、アクセシビリティ要件（キーボード操作、ARIA属性）
**Scale/Scope**: 既存イベント詳細画面の拡張、4つの新規コンポーネント、1つの新規サービス関数、18の機能要件

## Constitution Check

*GATE: Must pass before Phase 0 research. Re-check after Phase 1 design.*

### ✅ セキュリティ第一開発
- **準拠**: 読み取り専用UIのため、入力検証は不要。localStorageからのデータ読み取りのみ。
- **アクション**: なし

### ✅ 無料枠最適化
- **準拠**: 新規の外部依存関係なし、クライアントサイド処理のみ。
- **アクション**: なし

### ✅ 型安全性（非交渉可能）
- **準拠**: TypeScript strict mode使用、新規型定義（MemberAttendanceDetail等）を追加。
- **アクション**: すべての新規関数とコンポーネントに明示的な型定義を実装

### ✅ レスポンシブ・モバイルファースト設計
- **準拠**: Tailwind CSSでモバイルファースト、320px～対応。
- **アクション**: アコーディオン、フィルタ、検索UIをすべてのビューポートでテスト

### ✅ サーバーレスアーキテクチャ
- **準拠**: クライアントサイドのみの機能、API Route不要。
- **アクション**: なし

### ✅ アクセシビリティ基準
- **準拠**: アコーディオンにARIA属性（aria-expanded, aria-controls）、キーボード操作（Enter/Space）。
- **アクション**: すべてのインタラクティブ要素でキーボードナビゲーションとフォーカス表示を実装

### ✅ 日本語対応とローカライゼーション
- **準拠**: UIテキストすべて日本語、ドキュメントも日本語。
- **アクション**: メッセージテキスト（「メンバーがいません」等）をすべて日本語で実装

### ✅ テスト駆動開発（TDD）
- **準拠**: Red-Green-Refactorサイクルで実装。
- **アクション**: すべての新規関数・コンポーネントでテストファーストを厳守

### ✅ パフォーマンス基準
- **準拠**: 100人以上で3秒以内、クライアントサイドフィルタリング・ソート。
- **アクション**: パフォーマンステストを実装（100人のモックデータ）

### 🎯 評価結果
**ステータス**: ✅ **すべての憲法原則に準拠**

**違反**: なし

**複雑性の正当化**: 不要

## Project Structure

### Documentation (this feature)

```text
specs/007-event-member-attendance/
├── spec.md              # 機能仕様書（完了）
├── plan.md              # このファイル（実装計画）
├── research.md          # Phase 0 output
├── data-model.md        # Phase 1 output
├── quickstart.md        # Phase 1 output
├── contracts/           # Phase 1 output
│   └── attendance-service.md
├── tasks.md             # Phase 2 output (/speckit.tasks command)
└── checklists/          # 品質チェックリスト
    └── requirements.md  # 仕様書品質チェックリスト（完了）
```

### Source Code (repository root)

```text
attendance-hub/
├── app/                    # Next.js App Router
│   ├── [org]/             # 団体別ルート
│   │   └── events/
│   │       └── [id]/
│   │           └── page.tsx  # ⚠️ 拡張対象：イベント詳細ページ
│   ├── layout.tsx         # ルートレイアウト
│   └── globals.css        # Tailwind CSS
├── components/            # 再利用可能なコンポーネント
│   └── event-detail/      # ✨ 新規：イベント詳細関連コンポーネント
│       ├── group-attendance-accordion.tsx     # アコーディオンコンポーネント
│       ├── member-attendance-list.tsx         # 個人リストコンポーネント
│       └── attendance-filters.tsx             # フィルタ/ソート/検索UI
├── lib/                   # ビジネスロジック・ユーティリティ
│   ├── storage.ts        # localStorage操作
│   ├── group-service.ts  # グループ関連ロジック
│   ├── attendance-service.ts  # ⚠️ 拡張対象：出欠登録関連ロジック
│   ├── event-service.ts  # イベント関連ロジック
│   ├── validation.ts     # Zodスキーマ
│   └── date-utils.ts     # 日付フォーマット
├── types/                 # TypeScript型定義
│   └── index.ts          # ⚠️ 拡張対象：新規型定義を追加
├── contexts/              # Reactコンテキスト
│   └── organization-context.tsx  # 団体コンテキスト
├── __tests__/            # テスト
│   ├── app/
│   │   └── [org]/events/[id]/page.test.tsx  # イベント詳細ページテスト
│   ├── components/
│   │   └── event-detail/  # ✨ 新規：コンポーネントテスト
│   │       ├── group-attendance-accordion.test.tsx
│   │       ├── member-attendance-list.test.tsx
│   │       └── attendance-filters.test.tsx
│   └── lib/
│       └── attendance-service.test.ts  # ⚠️ 拡張対象：新規関数のテスト
└── specs/                # 機能仕様・設計ドキュメント
    └── 007-event-member-attendance/  # このフィーチャー
```

**Structure Decision**:
- **プロジェクトタイプ**: Next.js App Routerを使用したWebアプリケーション
- **拡張戦略**: 既存のイベント詳細ページと attendance-service.ts を拡張
- **新規コンポーネント**: `components/event-detail/` に再利用可能なコンポーネントを配置
- **テストの場所**: `__tests__/` 配下に既存の構造に従って配置

## Complexity Tracking

> **Fill ONLY if Constitution Check has violations that must be justified**

**該当なし**: すべての憲法原則に準拠しているため、複雑性の正当化は不要。

---

## Phase 完了報告

### ✅ Phase 0: Outline & Research（完了）

**成果物**:
- ✅ [research.md](./research.md) - 技術調査完了

**調査項目**:
1. ✅ アコーディオンUIの実装パターン → React useState + Tailwind CSS
2. ✅ アクセシビリティ対応 → WAI-ARIA準拠
3. ✅ 大量データのパフォーマンス最適化 → React.useMemo + 段階的レンダリング
4. ✅ フィルタリング・ソート・検索 → すべて同一階層、リアルタイム適用
5. ✅ 日本語文字列のソート → localeCompare('ja')
6. ✅ エラー処理とエッジケース → 明示的なメッセージ + フォールバックUI

**結果**: すべての技術的不明点を解決。Phase 1に進む準備完了。

---

### ✅ Phase 1: Design & Contracts（完了）

**成果物**:
- ✅ [data-model.md](./data-model.md) - データ構造と型定義
- ✅ [contracts/attendance-service.md](./contracts/attendance-service.md) - サービス関数のAPI契約
- ✅ [quickstart.md](./quickstart.md) - 開発環境セットアップ手順
- ✅ CLAUDE.md更新 - Agent contextに技術情報を追加

**新規型定義**:
1. ✅ **MemberAttendanceDetail**: メンバー情報と出欠ステータスを結合
2. ✅ **AttendanceFilterStatus**: フィルタオプション（all/attending/maybe/notAttending/unregistered）
3. ✅ **AttendanceSortBy**: ソート種類（name/status）

**新規関数設計**:
1. ✅ **getGroupMemberAttendances()**: グループメンバーの出欠詳細を取得
   - 8つのテストケース定義済み
   - パフォーマンス目標: 100人で50ms以内

**Constitution Check (Phase 1後)**:
- ✅ すべての原則に引き続き準拠
- ✅ 設計により新たな違反は発生せず

---

## 次のステップ

### Phase 2: タスク生成（/speckit.tasks）

以下のコマンドで実装タスクを生成:

```bash
/speckit.tasks
```

**期待される成果物**:
- `tasks.md`: 優先順位付き実装タスクリスト
- 各タスクはTDD（Red-Green-Refactor）に従って記述

### Phase 3: 実装（/speckit.implement）

tasks.mdに基づいて実装を開始:

```bash
/speckit.implement
```

**実装順序**:
1. サービス層（lib/attendance-service.ts）
2. コンポーネント層（components/event-detail/）
3. 統合（app/[org]/events/[id]/page.tsx）

**TDD原則**:
- Red: まず失敗するテストを書く
- Green: テストを通す最小限の実装
- Refactor: 動作を保ちながらコードを改善

---

## 参考資料

### このフィーチャーのドキュメント
- [spec.md](./spec.md) - 機能仕様書
- [research.md](./research.md) - 技術調査
- [data-model.md](./data-model.md) - データモデル
- [contracts/attendance-service.md](./contracts/attendance-service.md) - API契約
- [quickstart.md](./quickstart.md) - 開発環境セットアップ

### 品質チェックリスト
- [checklists/requirements.md](./checklists/requirements.md) - 仕様書品質チェックリスト

### プロジェクト憲法
- [constitution.md](../../.specify/memory/constitution.md) - 開発原則とガバナンス
